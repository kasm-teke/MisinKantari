<UserControl x:Class="WPF_LoginForm.Views.FitatGirisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_LoginForm.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">

    
    <Grid>


        <Grid.RowDefinitions>
            <RowDefinition Height="*" MaxHeight="60"/>
            <RowDefinition Height="*" MaxHeight="50"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="*" MaxHeight="50"/>

        </Grid.RowDefinitions>

        <Grid Grid.Row="2">
            
            
            
        
        
            <Grid.ColumnDefinitions >
                <!-- Navigation Menu -->
                <ColumnDefinition Width="1*"/>
                <!--Content Section-->
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>


            <RadioButton Style="{StaticResource menuButton}"
                             Tag="{StaticResource color13}" 
                             Command="{Binding OpenDatePickerCommand}"  >
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" >
                    <TextBox  MinWidth="100"  Text="{Binding SelectedDate, StringFormat=\{0:MM/dd/yyyy\}}" IsReadOnly="True" />
                   
                    <fa:IconImage Icon="CalendarMinus" Style="{StaticResource menuButtonIcon}" HorizontalAlignment="Left"/>


                </StackPanel>
                
                </RadioButton>

            
            
            <!-- Pop-up Calendar -->
            <Popup x:Name="PopupCalendar" PlacementTarget="{Binding ElementName=DatePickerButton}" StaysOpen="False" IsOpen="{Binding IsCalendarOpen}" AllowsTransparency="True">
                <Border Background="White" BorderBrush="Black" BorderThickness="1">
                    <Calendar SelectedDate="{Binding SelectedDate, Mode=TwoWay}" />
                </Border>
            </Popup>

            <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Load Data" Command="{Binding LoadDataCommand}" Width="100" Height="30" Margin="5" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}" />
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="5" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
            <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="5" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
